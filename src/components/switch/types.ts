// 定义开的接口
export interface SwitchOn{
    // 图标
    icon:string,
    // 名称可选
    name?:string,
    // 默认颜色
    color:string,
    // 选中颜色
    activeColor:string,
}
export interface SwitchOff{
    // 图标
    icon:string,
    // 名称可选
    name?:string,
    // 默认颜色
    color:string,
    // 选中颜色
    activeColor:string,
}